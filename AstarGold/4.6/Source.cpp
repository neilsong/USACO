#include <bits/stdc++.h>
using namespace std;
int n, m;
vector<vector<int>> adj;
vector<int> parent;
int main(void) {
	cin >> n >> m;
	adj.resize(n*m);
	parent.resize(n * m);
	for (int i = 0; i < n; i++) {
		for (int j = 0; j < m; j++) {
			if(i > 0) 
		}
	}
}