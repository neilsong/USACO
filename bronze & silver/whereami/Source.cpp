#include <bits/stdc++.h>
using namespace std;

int main(void) {
	string temp, real;
	getline(cin, temp);
	getline(cin, real);
	bool outputed = 0;
	set<string> 
	for (int i = 1; i < real.length(); i++) {
		for (int j = 0; j < real.length(); j++) {
			
		}
	}
}